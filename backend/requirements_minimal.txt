# Core dependencies (install mediapipe without deps to avoid matplotlib)
opencv-python==4.10.0.84
numpy<2.0
protobuf<4
absl-py
attrs
flatbuffers
# Install mediapipe last with --no-deps flag via separate command

# Web framework
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
replicate
pillow==11.1.0
